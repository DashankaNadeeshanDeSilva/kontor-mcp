# fixtures

Test invoices: the official XRechnung test suite (fetched by `tools/fetch-artifacts`, Task 0.2), ZUGFeRD samples, and hand-made broken invoices. Every third-party file must have a provenance entry in `packages/rules/PROVENANCE.md`.
