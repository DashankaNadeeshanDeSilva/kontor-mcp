# Hand-off — 2026-08-25 (end of Phase 2 tasks 2.1–2.6)

Written at the session boundary before Task 2.7. Read this first in the next session, then the files it points to.

## 1. State

- Branch `main`, pushed; CI green on all 7 jobs at `1c93863`; `9b59747` (Task 2.6) pushed last — check its run before starting.
- Tag `v0.1.0` = Phase 1 exit. No new tag yet (v0.9 comes after 2.7 + 2.8).
- Test count: rules 10 · core 259 · client 1 · server 24 = **294**, `pnpm lint` clean, `pnpm build` clean.
- Claude Desktop is wired to `packages/server/dist/bin.js`; ⌘Q + reopen after `pnpm build`.

### Commits this session (oldest → newest)

| Commit | Task | What |
|---|---|---|
| `c35b09e` | 2.1 | Plausibility layer `KONTOR-PLAUS-*` (22 ids) as third validation layer; 34 targeted cases |
| `f4b96b3` | 2.2 | `audit_invoice` — report + accept/review/reject; goldens; F5/F9/F10 folded in |
| `5fc265f` | 2.3 | `generate_invoice` — XRechnung 3.0 UBL from `InvoiceInput`, fail-honest; 50-seed property test |
| `fddf4ad` | 2.4 | `convert_invoice` — full-model UBL/CII serializers, loss report, HTML preview; suite-wide round trip |
| `97507d8`, `b9c2cf6`, `eaa1a7f` | CI | Windows fixes: `.gitattributes` (LF for source/tests/goldens, `-text` for artefacts/fixtures/samples); `fileURLToPath` for golden paths (the real cause) |
| `5e0aabc`, `1c93863` | 2.5 | `check_obligations` — sourced legal timeline (`packages/rules/legal/timeline.json`), decision tree, 15 scenarios |
| `9b59747` | 2.6 | Reference resources (rules index, 13 code lists, cheatsheet), 3 prompts, `list_capabilities` |

### Decisions added

D-032 … D-039 in `docs/DECISIONS.md` (decimal.js half-up + 1-cent warning policy; ibantools; plausibility never changes the official verdict; audit decision table; `InvoiceInput` shape + no repair loop; serializers = inverse of parse maps with 3 documented round-trip exceptions; legal facts as data with sources; code lists extracted + guarded).

## 2. Tools / resources / prompts now available

Tools: `parse_invoice`, `validate_invoice`, `audit_invoice`, `explain_rule`, `generate_invoice` (writes), `convert_invoice` (writes), `check_obligations`, `list_capabilities`.
Resources: `kontor://samples/{name}`, `kontor://reference/rules`, `kontor://reference/codelists/{list}`, `kontor://reference/cheatsheet`.
Prompts: `audit-incoming-invoice`, `draft-supplier-rejection`, `create-invoice-interview`.

## 3. Open items

Owner actions (manual, Claude Desktop after `pnpm build` + restart):
- Task 2.2 AC: an `audit_invoice` run should read like something an AP clerk can act on; check F6 (disclaimer relayed — the server `instructions` now ask for it).
- Task 2.6 AC: `draft-supplier-rejection` + audit findings → sendable German e-mail draft.
- Push of this hand-off, then `docs/VERIFICATION-v0.9.md` when v0.9 is cut.

Deferred by design:
- F7 (Desktop never hands attachment bytes to stdio servers) → Streamable HTTP, Task 3.1.
- `generate_invoice` target `cii`/`zugferd-pdf` → Task 2.7 (CII writer exists: `modelToCii`).
- The Java KoSIT oracle (`pnpm oracle`) is not a CI job; in-process parity (89/89) is the accepted oracle (D-036).
- `purchaseOrderReference: "NA"` convention for sales-order-only sources not confirmed offline (D-037) — parser untouched, surfaced as `added` in the loss report.
- Legal timeline re-verification whenever § 27 UStG / BMF FAQ change: edit `timeline.json` + `lastVerified`, not code (D-038).

## 4. Things that bit us (don't repeat)

- `new URL(..., import.meta.url).pathname` breaks on Windows → always `fileURLToPath`. Golden/snapshot files must be LF (`.gitattributes` handles it); third-party dirs are `-text`.
- Python heredocs with `€`/umlauts need `LC_ALL=en_US.UTF-8` in this shell, and a push chain must be gated on `pnpm lint` *and* tests (one commit slipped through without docs; fixed in `1c93863`).
- `grep -c` returning 0 exits 1 — don't use it inside `&&` chains.
- Stale `@kontor-mcp/rules` build breaks core tests → `pnpm build` before `pnpm -r test` when in doubt.

## 5. Kick-off brief for the next session (copy-paste)

> You are continuing Kontor MCP (repo /Users/dashankadesilva/Drive/Projects/Kontor-MCP, branch main, HEAD 9b59747 or later). Phase 2 tasks 2.1–2.6 are done and pushed (see docs/HANDOFF-2026-08-25.md — read it first, then CLAUDE.md, docs/PRD.md §5.5 T5 `zugferd-pdf` + §6 NFRs, docs/IMPLEMENTATION_PLAN.md Task 2.7 and §5 Phase 3, docs/DECISIONS.md D-021/D-022/D-026 and D-032–D-039).
>
> Hard rules (CLAUDE.md): money math decimal.js only · XXE disabled · never a runtime network call (packages/core/test/sovereignty.test.ts must pass) · Zod for all tool I/O, structuredContent + text · findings use the Finding interface · our checks are KONTOR-* · touching packages/rules artefacts → PROVENANCE.md · macOS FS case-insensitive · commits under my name only, no Co-Authored-By · TDD (failing tests first), `pnpm -r test` + `pnpm lint` green before every commit, conventional commits, CHANGELOG + DECISIONS entries, verify library versions against live docs.
>
> Toolchain: `export PATH="$HOME/.local/bin:$PATH"` (pnpm 10 via corepack), Node 22, vitest, Biome; veraPDF 1.30.2 via Homebrew (`tools/verapdf.sh`), no Java. CI: 7 jobs incl. Windows — use fileURLToPath for any path from import.meta.url. Claude Desktop is wired to packages/server/dist/bin.js (⌘Q + reopen after `pnpm build`).
>
> Start with **Task 2.7 — ZUGFeRD PDF/A-3 generation** (`generate_invoice` target `zugferd-pdf`, D-022: custom pdf-lib path proven in tools/spike-pdfa3.ts): semantic model → CII (`modelToCii`, profile-aware) → visual PDF from the `renderHtmlPreview` layout (embedded fonts, PDF/A-safe) → PDF/A-3 with `factur-x.xml` (AFRelationship, MIME) + Factur-X/ZUGFeRD XMP extension schema → embedded XML validated through the T2 pipeline (fail-honest `valid`). AC: veraPDF zero violations on the EN16931 sample; embedded XML passes full validation; round-trip through `detectInvoicePdf` + validation; one external validator check documented in docs/CONFORMANCE.md; profile parameter covered. Before coding, brainstorm the design (PDF rendering approach without a browser, font embedding, XMP template, profile matrix) and get my OK. Then Task 2.8 (README v1 + repo polish, tag v0.9), then Phase 3.
