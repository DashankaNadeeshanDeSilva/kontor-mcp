/**
 * @kontor-mcp/rules — bundled standards artifacts and the rule knowledge base.
 * Placeholder for Task 0.1; real content arrives with Tasks 0.2/1.5.
 */
export const RULES_PACKAGE = "@kontor-mcp/rules" as const;

/** Versions of bundled standards; populated in Task 0.2 (see PROVENANCE.md). */
export const bundledStandards = {
  xrechnung: "unpinned",
  en16931: "unpinned",
} as const;
